import { describe } from "mocha";
import { delayPromise, roundToDp } from "./utils";

describe(__filename, () => {
  describe("delayPromise()", () => {
    // TODO
    it("TODO", () => {
      delayPromise(1);
    });
  });

  describe("roundToDp()", () => {
    // TODO
    it("TODO", () => {
      roundToDp(0, 0);
    });
  });
});
